<style>
  .item {
    display: flex;
    gap: 1em;

    & .iconbox {
      display: flex;
      align-items: flex-end;
    }

    & :global(svg) {
      color: var(--color-dark);
      height: 1.2em;
    }

    @media print {
      & .iconbox {
        align-items: flex-start;
      }
    }
  }
</style>
<div class="item">
  <div class="iconbox">
    <slot name="icon" />
  </div>
  <div>
    <slot />
  </div>
</div>
